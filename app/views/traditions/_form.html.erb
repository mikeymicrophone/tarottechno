<%= form_for(@tradition) do |f| %>
  <% if @tradition.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tradition.errors.count, "error") %> prohibited this tradition from being saved:</h2>

      <ul>
      <% @tradition.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
