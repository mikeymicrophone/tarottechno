<p>
  <strong>Tradition:</strong>
  <%= link_to @deck.tradition.name, @deck.tradition %>
</p>

<p>
  <%= @deck.name %>
</p>

<%= content_tag_for :header, @deck, :cards do %>
  ~cards~
<% end %>
<%= div_for @deck, :cards_in do %>
  <% @deck.cards.sequenced.each do |card| %>
    <%= div_for card do %>
      <%= link_to card.name, card %>
    <% end %>
  <% end %>
<% end %>
