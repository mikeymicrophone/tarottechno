<%= link_to 'card names', card_names_suit_path(@suit) %>

<p>
  <strong>Name:</strong>
  <%= @suit.name %>
</p>

<p>
  <strong>Symbol:</strong>
  <%= @suit.symbol %>
</p>

<%= link_to 'interfere', edit_suit_path(@suit) %> |
<%= link_to 'Back', suits_path %>
