<%= form_for(@spread) do |f| %>
  <% if @spread.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@spread.errors.count, "error") %> prohibited this spread from being saved:</h2>

      <ul>
      <% @spread.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :tradition_id %><br>
    <%= f.select :tradition_id, options_from_collection_for_select(Tradition.all, :id, :name, @spread.tradition_id), :include_blank => 'Select' %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
